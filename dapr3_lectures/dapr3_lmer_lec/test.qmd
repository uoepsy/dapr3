---
title: "Untitled"
format: html
---

```{r}
#| results: "asis"
block1_name = "intro to linear model<br>describing linear relationships between predictors and an outcome"
block1_lecs = c("a straight line",
                "more than one predictor",
                "tests of the linear relationships",
                "comparing different linear models",
                "example")
block2_name = "more linear models<br>working with categorical predictors, model assumptions"
block2_lecs = c("when X is a categorical variable - differences from a reference level",
                "when X is a categorical variable - differences from the average",
                "assumptions of linear models",
                "robust estimation of uncertainty (bootstrapping)",
                "example")

source("https://raw.githubusercontent.com/uoepsy/junk/main/R/course_table.R")
course_table(block1_name,block2_name,block1_lecs,block2_lecs,week=1)
```

